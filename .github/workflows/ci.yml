name: CI

on:
  push:
    branches: [ main , CItesting ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2

    - name: Setup Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '21'

    # move to next project
    - name: Move to project
      run: cd retrograde-dark-orbit
      
    # Install dependencies
    - name: Install Dependencies
      run: npm ci

    # Run Jest tests
    - name: Run Tests
      run: npm run test